# Database Connection String (PostgreSQL)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/second_brain?schema=public"

# JWT Secret Key for session signing
JWT_SECRET="super-secret-random-key-change-in-production-123456!"

# Local Storage Directory for templates and project files
STORAGE_DIR="./storage"
